spring.application.name=cadastro-tags

server.port=8082

# URL de conexão com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/cadastro_tags?useSSL=false&serverTimezone=UTC

# Credenciais do banco de dados
spring.datasource.username=root
spring.datasource.password=Dev1310#5

# Driver do banco de dados
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialeto do Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configurações adicionais do Hibernate (opcional)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true